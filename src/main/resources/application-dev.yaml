spring:

  datasource:
    url: jdbc:mysql://localhost:3310/store_api?createDatabaseIfNotExist=true
    username: root
    password: nanaosei
  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

clientUrl: http://localhost:4545